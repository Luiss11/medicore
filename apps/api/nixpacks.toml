[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm@9",
  "cd /app && pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "cd /app/apps/api && pnpm build"
]

[start]
cmd = "cd /app/apps/api && node dist/server.js"
